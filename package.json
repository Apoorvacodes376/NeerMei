{
  "name": "neermei",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"backend,frontend,ml\" --prefix-colors \"cyan,magenta,yellow\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run ml\"",
    "ml": "cd ml-service && uvicorn main:app --reload --port 8000",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix bridge && pip install -r ml-service/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
